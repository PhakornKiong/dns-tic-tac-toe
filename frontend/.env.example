# DNS Server Configuration
# The host where the Go DNS server is running
DNS_HOST=127.0.0.1

# The port where the Go DNS server is listening
# Use 53 for standard DNS (requires root/sudo)
# Use 5353 for non-privileged port
DNS_PORT=53

# The DNS zone/domain used by the game
DNS_ZONE=game.local
